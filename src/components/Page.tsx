import styled from 'styled-components'

const Page = styled.div`
  min-height: 100vh;
  background: radial-gradient(circle at 20% 10%, rgba(212, 175, 55, 0.08), transparent 40%),
    radial-gradient(circle at 80% 80%, rgba(154, 27, 27, 0.12), transparent 45%), var(--ink);
  padding-bottom: 4rem;
`

export default Page
